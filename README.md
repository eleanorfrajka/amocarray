# AMOC array

A repository for handling AMOC observing array data.

Intention: to simply the process of accessing data from MOC transport mooring arrays, unifying the format (where possible) and adding attributes.

## Install

Install from the command line as:
```
pip install -e .
```